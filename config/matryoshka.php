<?php


return [
    /*
     * The tags that this cache uses. Defaults to views.
     */
    'default_cache_tags' => 'views',

    /*
     * Whether to flush the cache if on local env.
     */
    'flush_cache_on_local' => true,
];
